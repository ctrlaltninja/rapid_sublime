/// foo1(x)
/// baz,boz = foobar(x, y)
/// Foo.bar(x, y)
/// some description about it
/// baz,boz = Foo.bar(x, y)
/// blabla
/// a1,... = variadic_return(x, y)
/// r1, r2, r3, r4 = multiple_returns(cbuffer, index)